function (doc, meta) {
  if(doc.updated) {
    if(doc.sticky && doc.sticky == true)
      emit([doc.sticky, doc.updated], doc, null);
  }

  if(doc.updated && !doc.sticky) {
    emit(doc.updated, doc, null);
  }
}